# Dependencias para trabajar los notebooks y correr los tests. El target `notebooks` de
# la imagen las instala; el target `pipeline` no las necesita.
-r requirements.txt

jupyterlab==4.4.5
ipykernel==6.30.0
matplotlib==3.10.6
seaborn==0.13.2

# Tests (tests/): pytest sobre las funciones puras de cada etapa.
pytest==8.4.1
httpx==0.28.1     # TestClient de FastAPI en tests/test_web.py

# El enriquecimiento geoespacial NO necesita osmnx/geopandas: contar POIs en un
# radio se resuelve con una query Overpass (urllib de la stdlib) + cKDTree de scipy.
# osmnx arrastra GDAL/shapely/pyproj y sólo se justifica para análisis de red vial.
